# .gitignore
# ----------------------------------------------------
# .gitignore가 실행되지 않을때 다음 순서대로 처리하자.
# $ git rm -r --cached.
# $ git add .
# $ git commit -m "fixed untracked files"
# ----------------------------------------------------

# scss 변환시 해당 내용 정보처리에 대한 자료
**/*.css.map

# prepros이용하여 생긴 config파일
**/prepros.config

# node-module 삭제
**/node_modules
node_modules
**/package-lock.json

# .vscode
**/*.vscode